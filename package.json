{
  "name": "wmf-viewer",
  "displayName": "WMF/EMF Viewer",
  "publisher": "congduan",
  "description": "Preview WMF and EMF images in VSCode",
  "version": "0.0.6",
  "icon": "src/resources/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/congduan/WmfEmfViewer.git"
  },
  "homepage": "https://github.com/congduan/WmfEmfViewer",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Visualization"
  ],
  "activationEvents": [
    "onCommand:wmfViewer.preview",
    "onFileSystem:file"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "wmfViewer.preview",
        "title": "Preview WMF/EMF Image"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "wmfViewer.preview",
          "when": "resourceExtname == .wmf || resourceExtname == .emf"
        }
      ]
    },
    "customEditors": [
      {
        "viewType": "wmfViewer.editor",
        "displayName": "WMF/EMF Viewer",
        "selector": [
          {
            "filenamePattern": "*.wmf"
          },
          {
            "filenamePattern": "*.emf"
          }
        ]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "npm run build:bundle && npm run compile",
    "build:bundle": "node src/build/build-browser-bundle.js",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run build && (if [ -f '.eslintrc.js' ] || [ -f '.eslintrc.json' ]; then npm run lint; else echo 'No ESLint configuration found, skipping linting'; fi)",
    "lint": "eslint src --ext ts",
    "test": "node test/runTest.js",
    "package": "vsce package --readme-path MARKETPLACE.md",
    "publish": "vsce publish --readme-path MARKETPLACE.md"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^9.1.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.75.0",
    "@typescript-eslint/eslint-plugin": "^5.30.0",
    "@typescript-eslint/parser": "^5.30.0",
    "@vscode/test-electron": "^2.1.5",
    "@vscode/vsce": "^3.9.2",
    "eslint": "^8.20.0",
    "glob": "^8.0.3",
    "mocha": "^10.0.0",
    "typescript": "^4.7.4"
  }
}